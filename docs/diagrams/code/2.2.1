flowchart TD
    subgraph User_Device["User Device"]
        U["User"]
        CAM["Device Camera"]
        MA["Mobile App (React Native)"]
    end

    subgraph Hardware["Hardware"]
        MC["ESP32 Microcontroller"]
        IMU["IMU Sensor"]
    end

    subgraph Cloud_Backend["Cloud Backend"]
        FB["Firebase (Auth & Firestore)"]
        GCP["Google Cloud Platform (Hosting Infrastructure)"]
    end

    U -- uses --> MA
    CAM -- "Camera Feed" --> MA
    MA -- "HTTPS/REST (Auth, Logging)" --> FB
    MA -- "WebSocket/Wi-Fi (IMU Data Stream)" --> MC
    MC -- "I²C (Sensor Data)" --> IMU
    FB -- "Hosted on" --> GCP
